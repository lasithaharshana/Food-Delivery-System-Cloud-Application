apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restaurant-mysql-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-mysql
  template:
    metadata:
      labels:
        app: restaurant-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: restaurant_db
            - name: MYSQL_USER
              value: restaurant_user
            - name: MYSQL_PASSWORD
              value: restaurant_pass
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: restaurant-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-mysql
spec:
  selector:
    app: restaurant-mysql
  ports:
    - port: 3306
      targetPort: 3306


