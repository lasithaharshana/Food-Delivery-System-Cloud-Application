spring.application.name=apigateway
server.port=8080

# Service URLs for Docker environment
auth.service.url=http://authservice:8081
order.service.url=http://orderservice:9094
food.service.url=http://foodservice:9093

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Logging
logging.level.com.deliverysystem.apigateway=INFO
logging.level.org.springframework.security=INFO

# CORS Configuration for production
cors.allowed.origins=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://frontend:80,http://frontend:3000
cors.allowed.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed.headers=*
cors.allow.credentials=true

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# Actuator endpoints for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
